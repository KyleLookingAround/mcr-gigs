# Vite builds the static site to dist/. Edge functions are auto-detected from
# netlify/edge-functions and run on Deno (separate from the Vite build).

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
